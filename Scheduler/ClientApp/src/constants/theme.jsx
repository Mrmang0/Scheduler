import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#114B5F',
    },
    secondary: {
      main: '#1F7A8C',
    },
    background: {
      default: '#e1e1e1',
    },
  },
});

export default theme;
